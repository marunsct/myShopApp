class Cart {
  constructor(quantity, price, title, totalAmount) {
    this.quantity = quantity;
    this.price = price;
    this.title = title;
    this.totalAmount = totalAmount;
  }
}

export default Cart;
