class Cart {
  constructor(quantity, price, title, totalAmount, url) {
    this.quantity = quantity;
    this.price = price;
    this.title = title;
    this.totalAmount = totalAmount;
    this.url = url;
  }
}

export default Cart;
